{
  "note": "Files excused from aglyn/no-raw-mui-table, each with its reason. The rule reports a row whose file no longer draws a raw MUI Table, so this list cannot outlive what it excuses. It may shrink and must never grow: a new raw table is refused, not recorded.",
  "files": []
}
